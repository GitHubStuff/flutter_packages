import 'package:flutter/material.dart';

class Const {
  static const EdgeInsets padding = EdgeInsets.all(4.0);
  static const EdgeInsets seperatorPadding = EdgeInsets.only(top: 4, bottom: 4);
  static const fontSize = 400.0;
  static const amIndex = 0;
  static const pmIndex = 1;
}
